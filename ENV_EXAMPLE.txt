# Backend Environment Variables
# Copy nội dung này vào file .env trong thư mục user-registration-api
# Hoặc cấu hình trực tiếp trên hosting platform

# MongoDB Connection String
# Example: mongodb://localhost:27017/user-registration
# Or MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGO_URI=your_mongodb_connection_string_here

# Server Port (default: 3000)
# Trên Render/Railway, PORT sẽ được tự động set
PORT=3000

# Frontend Origin for CORS
# For local development: http://localhost:3001
# For production: https://your-frontend-domain.com
FRONTEND_ORIGIN=http://localhost:3001

